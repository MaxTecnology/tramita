# Fase 8d: Relatório PDF — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Endpoint `GET /clients/:clientId/report?month=YYYY-MM` que gera um PDF com cabeçalho da org, resumo executivo e tabela de tarefas do período, com cache Redis de 1h.

**Architecture:** Puppeteer renderiza HTML → PDF em memória. O serviço monta o HTML com dados do Prisma (tarefas movidas no período + histórico), chama Puppeteer headless, armazena o PDF em cache Redis por 1h (chave `report:{clientId}:{month}`). O endpoint verifica cache antes de gerar. Frontend interno adiciona botão "Exportar"; portal já tem a página Reports com o botão de download.

**Tech Stack:** `puppeteer-core` + `@puppeteer/browsers`, Redis (ioredis — já instalado), Prisma.

---

## File Map

**Backend — criar:**
- `apps/api/src/modules/reports/reports.service.ts` — `generateReport(clientId, orgId, month)`
- `apps/api/src/modules/reports/reports.routes.ts` — `GET /clients/:clientId/report`
- `apps/api/src/modules/reports/reports.service.test.ts` ← OBRIGATÓRIO

**Backend — modificar:**
- `apps/api/src/server.ts` — registrar `reportsRoutes`

**Frontend — modificar:**
- `apps/web/src/pages/app/Dashboard.tsx` — adicionar botão "Exportar relatório" nos cards de board

---

## Task 1: Backend — reports service (TDD)

**Files:**
- Create: `apps/api/src/modules/reports/reports.service.ts`
- Create: `apps/api/src/modules/reports/reports.service.test.ts`

- [ ] **Step 1: Instalar dependência**

```bash
pnpm --filter api add puppeteer
```

- [ ] **Step 2: Criar o teste PRIMEIRO**

`apps/api/src/modules/reports/reports.service.test.ts`:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
import {
  createTestPlan,
  createTestOrg,
  createTestUser,
  createTestClient,
  createTestBoard,
  createTestColumn,
  createTestTask,
} from '@/test/helpers'
import { generateReport } from '@/modules/reports/reports.service'
import { redis } from '@/lib/redis'

// Mock puppeteer to avoid launching real browser in tests
vi.mock('puppeteer', () => ({
  default: {
    launch: vi.fn().mockResolvedValue({
      newPage: vi.fn().mockResolvedValue({
        setContent: vi.fn(),
        pdf: vi.fn().mockResolvedValue(Buffer.from('%PDF-1.4 mock')),
      }),
      close: vi.fn(),
    }),
  },
}))

beforeEach(() => {
  vi.restoreAllMocks()
})

describe('generateReport', () => {
  it('generates PDF buffer for the correct month', async () => {
    const plan = await createTestPlan()
    const org = await createTestOrg(plan.id)
    const user = await createTestUser(org.id)
    const client = await createTestClient(org.id)
    const board = await createTestBoard(org.id, client.id)
    const col = await createTestColumn(board.id, { position: 0 })
    await createTestTask(col.id, user.id, { title: 'Tarefa do mês' })

    const result = await generateReport(client.id, org.id, '2026-06')

    expect(result).toBeInstanceOf(Buffer)
    expect(result.length).toBeGreaterThan(0)
  })

  it('returns cached PDF on second call without re-generating', async () => {
    const puppeteer = await import('puppeteer')
    const launchSpy = vi.spyOn(puppeteer.default, 'launch')

    // Pre-populate cache
    const cacheKey = `report:client-cached:2026-06`
    await redis.set(cacheKey, Buffer.from('%PDF cached').toString('base64'), 'EX', 3600)

    const plan = await createTestPlan()
    const org = await createTestOrg(plan.id)
    const client = await createTestClient(org.id)

    // Override client id to match cache key — we'll use a direct service call
    // Instead, test by calling generateReport twice and checking launch is called once
    const plan2 = await createTestPlan()
    const org2 = await createTestOrg(plan2.id)
    const user2 = await createTestUser(org2.id)
    const client2 = await createTestClient(org2.id)
    const board2 = await createTestBoard(org2.id, client2.id)
    const col2 = await createTestColumn(board2.id, { position: 0 })
    await createTestTask(col2.id, user2.id)

    await generateReport(client2.id, org2.id, '2026-06')
    await generateReport(client2.id, org2.id, '2026-06')

    // puppeteer.launch should be called only once (second call uses cache)
    expect(launchSpy).toHaveBeenCalledTimes(1)
  })
})
```

- [ ] **Step 3: Rodar e confirmar que falha**

```bash
pnpm --filter api test src/modules/reports/reports.service.test.ts 2>&1 | tail -5
```

Expected: FAIL — módulo não encontrado.

- [ ] **Step 4: Criar `apps/api/src/modules/reports/reports.service.ts`**

```typescript
import puppeteer from 'puppeteer'
import { prisma } from '@/lib/prisma'
import { redis } from '@/lib/redis'
import { AppError } from '@/errors/AppError'

const CACHE_TTL = 3600 // 1 hour

function buildReportHtml(
  orgName: string,
  clientName: string,
  month: string,
  tasks: Array<{ title: string; status: string; priority: string; updatedAt: Date }>,
): string {
  const taskRows = tasks
    .map(
      (t) =>
        `<tr>
          <td>${t.title}</td>
          <td>${t.status}</td>
          <td>${t.priority}</td>
          <td>${new Date(t.updatedAt).toLocaleDateString('pt-BR')}</td>
        </tr>`,
    )
    .join('')

  return `<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<style>
  body { font-family: Arial, sans-serif; padding: 40px; color: #333; }
  h1 { color: #1d4ed8; }
  h2 { color: #6b7280; font-size: 14px; }
  table { width: 100%; border-collapse: collapse; margin-top: 24px; }
  th { background: #f3f4f6; padding: 8px 12px; text-align: left; font-size: 12px; }
  td { padding: 8px 12px; border-bottom: 1px solid #e5e7eb; font-size: 12px; }
  .summary { background: #eff6ff; border-radius: 8px; padding: 16px; margin: 24px 0; }
  .summary p { margin: 4px 0; font-size: 13px; }
</style>
</head>
<body>
  <h1>${orgName}</h1>
  <h2>Relatório mensal — ${month} — Cliente: ${clientName}</h2>

  <div class="summary">
    <p><strong>Total de tarefas no período:</strong> ${tasks.length}</p>
    <p><strong>Concluídas:</strong> ${tasks.filter((t) => t.status === 'DONE').length}</p>
    <p><strong>Em andamento:</strong> ${tasks.filter((t) => t.status !== 'DONE' && t.status !== 'CANCELLED').length}</p>
  </div>

  <table>
    <thead>
      <tr>
        <th>Tarefa</th>
        <th>Status</th>
        <th>Prioridade</th>
        <th>Última atualização</th>
      </tr>
    </thead>
    <tbody>${taskRows}</tbody>
  </table>

  <p style="margin-top:32px;font-size:11px;color:#9ca3af;">
    Gerado em ${new Date().toLocaleDateString('pt-BR')} — Tramita / AutoHubs
  </p>
</body>
</html>`
}

export async function generateReport(
  clientId: string,
  organizationId: string,
  month: string, // format: YYYY-MM
): Promise<Buffer> {
  // Validate month format
  if (!/^\d{4}-\d{2}$/.test(month)) throw new AppError(400, 'Formato de mês inválido. Use YYYY-MM')

  // Check Redis cache
  const cacheKey = `report:${clientId}:${month}`
  const cached = await redis.get(cacheKey).catch(() => null)
  if (cached) return Buffer.from(cached, 'base64')

  // Verify client belongs to org
  const client = await prisma.client.findFirst({
    where: { id: clientId, organizationId, isActive: true },
    include: { organization: { select: { name: true } } },
  })
  if (!client) throw new AppError(404, 'Cliente não encontrado')

  // Get tasks updated in the month
  const [year, mon] = month.split('-').map(Number)
  const from = new Date(year, mon - 1, 1)
  const to = new Date(year, mon, 0, 23, 59, 59, 999)

  const tasks = await prisma.task.findMany({
    where: {
      column: { board: { clientId, organizationId } },
      updatedAt: { gte: from, lte: to },
    },
    orderBy: { updatedAt: 'desc' },
    select: { title: true, status: true, priority: true, updatedAt: true },
  })

  // Generate PDF with Puppeteer
  const html = buildReportHtml(client.organization.name, client.name, month, tasks)

  const browser = await puppeteer.launch({ headless: true, args: ['--no-sandbox', '--disable-setuid-sandbox'] })
  const page = await browser.newPage()
  await page.setContent(html, { waitUntil: 'networkidle0' })
  const pdfBuffer = await page.pdf({ format: 'A4', printBackground: true })
  await browser.close()

  const buffer = Buffer.from(pdfBuffer)

  // Cache for 1h
  await redis.set(cacheKey, buffer.toString('base64'), 'EX', CACHE_TTL).catch(() => {})

  return buffer
}
```

- [ ] **Step 5: Rodar — deve passar**

```bash
pnpm --filter api test src/modules/reports/reports.service.test.ts --reporter=verbose 2>&1 | tail -12
```

Expected: 2 testes PASS.

**Se puppeteer falhar ao instalar ou mock não funcionar:** Verificar que o mock do puppeteer está no topo do arquivo (antes dos imports do service) — Vitest processa mocks antes da execução.

- [ ] **Step 6: Commit**

```bash
git -C /home/max/job/autohubs/tramita add apps/api/src/modules/reports/ pnpm-lock.yaml
git -C /home/max/job/autohubs/tramita commit -m "feat: reports service — PDF com Puppeteer + cache Redis 1h (TDD)"
```

---

## Task 2: Backend — reports route

**Files:**
- Create: `apps/api/src/modules/reports/reports.routes.ts`
- Modify: `apps/api/src/server.ts`

- [ ] **Step 1: Criar `apps/api/src/modules/reports/reports.routes.ts`**

```typescript
import type { FastifyInstance } from 'fastify'
import { verifyJWT } from '@/middlewares/verifyJWT'
import { requireRole } from '@/middlewares/requireRole'
import { AppError } from '@/errors/AppError'
import { generateReport } from './reports.service'

export async function reportsRoutes(app: FastifyInstance) {
  app.addHook('preHandler', verifyJWT)

  app.get('/clients/:clientId/report', {
    preHandler: [requireRole('ORG_ADMIN', 'ORG_MANAGER', 'CLIENT')],
  }, async (request, reply) => {
    const { clientId } = request.params as { clientId: string }
    const { month } = request.query as { month?: string }

    if (!month) throw new AppError(400, 'Parâmetro month é obrigatório (formato: YYYY-MM)')

    // CLIENT can only access own report
    const { role, sub, organizationId } = request.user
    if (role === 'CLIENT' && sub !== clientId) throw new AppError(403, 'Acesso negado')

    const pdfBuffer = await generateReport(clientId, organizationId!, month)

    return reply
      .header('Content-Type', 'application/pdf')
      .header('Content-Disposition', `attachment; filename="relatorio-${month}.pdf"`)
      .send(pdfBuffer)
  })
}
```

- [ ] **Step 2: Registrar em `apps/api/src/server.ts`**

Adicionar import:
```typescript
import { reportsRoutes } from '@/modules/reports/reports.routes'
```

Adicionar registro (após `attachmentsRoutes`):
```typescript
app.register(reportsRoutes)
```

- [ ] **Step 3: Rodar suite completa**

```bash
pnpm --filter api test 2>&1 | tail -5
```

Expected: todos os testes passando.

- [ ] **Step 4: Commit**

```bash
git -C /home/max/job/autohubs/tramita add apps/api/src/modules/reports/reports.routes.ts apps/api/src/server.ts
git -C /home/max/job/autohubs/tramita commit -m "feat: reports route — GET /clients/:clientId/report?month=YYYY-MM"
```

---

## Task 3: Frontend — botão exportar no Dashboard

**Files:**
- Modify: `apps/web/src/pages/app/Dashboard.tsx`

- [ ] **Step 1: Atualizar `apps/web/src/pages/app/Dashboard.tsx`**

Ler o arquivo atual. Adicionar botão "Exportar PDF" em cada card de board.

Adicionar função de download dentro de `Dashboard()`:
```typescript
async function handleExportReport(clientId: string, clientName: string) {
  const month = new Date().toISOString().slice(0, 7) // YYYY-MM
  try {
    const res = await api.get(`/clients/${clientId}/report?month=${month}`, {
      responseType: 'blob',
    })
    const url = URL.createObjectURL(new Blob([res.data], { type: 'application/pdf' }))
    const a = document.createElement('a')
    a.href = url
    a.download = `relatorio-${clientName}-${month}.pdf`
    a.click()
    URL.revokeObjectURL(url)
  } catch {
    alert('Relatório não disponível para este período.')
  }
}
```

No JSX, dentro de cada `<Card>` do board, adicionar um botão antes do fechamento do `<CardContent>`:
```typescript
<button
  type="button"
  onClick={(e) => {
    e.preventDefault()
    handleExportReport(board.client.id, board.client.name)
  }}
  className="mt-2 text-xs text-blue-500 hover:text-blue-700 hover:underline"
>
  Exportar relatório
</button>
```

- [ ] **Step 2: Verificar build**

```bash
pnpm --filter web build 2>&1 | tail -5
```

- [ ] **Step 3: Rodar testes**

```bash
pnpm --filter web test 2>&1 | tail -5
```

- [ ] **Step 4: Commit**

```bash
git -C /home/max/job/autohubs/tramita add apps/web/src/pages/app/Dashboard.tsx
git -C /home/max/job/autohubs/tramita commit -m "feat: botão exportar relatório PDF no Dashboard interno"
```

---

## Task 4: TASKS.md

- [ ] **Step 1: Rodar suites completas**

```bash
pnpm --filter api test 2>&1 | tail -5 && pnpm --filter web test 2>&1 | tail -5
```

- [ ] **Step 2: Atualizar TASKS.md**

Marcar no header da Fase 8:
```markdown
- [x] `reports.service.test.ts` — PDF gerado com dados do mês correto, cache Redis funciona
```

Marcar `### 8d — Relatório PDF` como `✅` e todos os itens como `[x]`.

Marcar também `## Fase 8 — Recursos Avançados` com `✅` (todos os sub-itens completos).

- [ ] **Step 3: Commit**

```bash
git -C /home/max/job/autohubs/tramita add docs/TASKS.md
git -C /home/max/job/autohubs/tramita commit -m "docs: Fase 8 completa — SSE, Busca, Anexos, PDF"
```

---

## Self-Review

### Spec coverage
| Requisito | Task |
|---|---|
| `reports.service.test.ts` — PDF com dados do mês correto, cache Redis | Task 1 |
| Puppeteer no docker-compose | Fora do escopo deste plano (Fase 9 — infra) |
| `GET /clients/:clientId/report?month=YYYY-MM` | Task 2 |
| Conteúdo: cabeçalho org, resumo executivo, tabela de tarefas | Task 1 (`buildReportHtml`) |
| Frontend interno: botão "Exportar relatório" | Task 3 |
| Portal: seção de relatórios | Já existente desde Fase 7 (`Reports.tsx`) |

### Placeholder scan
Sem placeholders — código completo em todos os steps. `buildReportHtml` em Task 1 gera o HTML real com org name, client name, task table e summary.

### Type consistency
`generateReport(clientId, orgId, month)` — assinatura consistente entre service e route.
Redis cache usa chave `report:{clientId}:{month}` — consistente entre generate (write) e route seria o mesmo se adicionarmos invalidação futura.
