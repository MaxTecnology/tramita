# Fase 8a: SSE — Tempo Real — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar Server-Sent Events para que o board do escritório e o portal do cliente atualizem em tempo real quando tarefas são movidas, criadas, atualizadas ou comentadas.

**Architecture:** Backend usa Redis Pub/Sub — cada mutação publica um evento no canal `board:{boardId}`; o endpoint SSE `GET /boards/:id/stream?token=<jwt>` cria um subscriber Redis dedicado por conexão e repassa os eventos para o cliente. Frontend usa um hook `useBoardStream` com `EventSource` nativo e reconnect com backoff exponencial; ao receber qualquer evento, invalida a query do board no TanStack Query.

**Tech Stack:** Backend: ioredis (pub/sub), Fastify v5 `reply.hijack()`. Frontend: `EventSource` nativo (sem lib extra), TanStack Query v5 `invalidateQueries`.

---

## File Map

**Backend — criar:**
- `apps/api/src/lib/sse.ts` — `publishBoardEvent(boardId, payload)` + tipo `SSEEvent`
- `apps/api/src/modules/stream/stream.routes.ts` — endpoint `GET /boards/:id/stream?token=<jwt>`
- `apps/api/src/modules/stream/stream.test.ts` ← OBRIGATÓRIO

**Backend — modificar:**
- `apps/api/src/modules/tasks/tasks.service.ts` — adicionar `board.id` nos selects + chamar `publishBoardEvent` após `createTask`, `moveTask`, `updateTask`
- `apps/api/src/modules/comments/comments.service.ts` — incluir `board.id` na query do task + chamar `publishBoardEvent` após `createComment`
- `apps/api/src/server.ts` — registrar `streamRoutes`

**Frontend — criar:**
- `apps/web/src/hooks/useBoardStream.ts` — SSE hook com reconnect backoff

**Frontend — modificar:**
- `apps/web/src/pages/app/Board.tsx` — chamar `useBoardStream(boardId)`
- `apps/web/src/pages/portal/Board.tsx` — chamar `useBoardStream(boardId)`

---

## Task 1: Backend — SSE lib + event emission nos serviços (TDD)

**Files:**
- Create: `apps/api/src/lib/sse.ts`
- Create: `apps/api/src/modules/stream/stream.test.ts`
- Modify: `apps/api/src/modules/tasks/tasks.service.ts`
- Modify: `apps/api/src/modules/comments/comments.service.ts`

- [ ] **Step 1: Criar o teste PRIMEIRO**

`apps/api/src/modules/stream/stream.test.ts`:
```typescript
import { describe, it, expect, vi, afterEach } from 'vitest'
import { app } from '@/test/setup'
import {
  createTestPlan,
  createTestOrg,
  createTestUser,
  createTestClient,
  createTestBoard,
  createTestColumn,
  createTestTask,
  getAuthHeader,
} from '@/test/helpers'
import { redis } from '@/lib/redis'
import { moveTask } from '@/modules/tasks/tasks.service'

afterEach(() => {
  vi.restoreAllMocks()
})

describe('SSE — event emission', () => {
  it('publishes task:moved event to board Redis channel after moveTask', async () => {
    const publishSpy = vi.spyOn(redis, 'publish').mockResolvedValue(0 as unknown as number)

    const plan = await createTestPlan()
    const org = await createTestOrg(plan.id)
    const user = await createTestUser(org.id)
    const client = await createTestClient(org.id)
    const board = await createTestBoard(org.id, client.id)
    const col1 = await createTestColumn(board.id, { position: 0 })
    const col2 = await createTestColumn(board.id, { position: 1 })
    const task = await createTestTask(col1.id, user.id)

    await moveTask(task.id, org.id, { columnId: col2.id, position: 0 }, { id: user.id, type: 'user' })

    expect(publishSpy).toHaveBeenCalledWith(
      `board:${board.id}`,
      expect.stringContaining('"task:moved"'),
    )
  })
})

describe('GET /boards/:id/stream', () => {
  it('returns 401 without token', async () => {
    const res = await app.inject({ method: 'GET', url: '/boards/xxx/stream' })
    expect(res.statusCode).toBe(401)
  })

  it('returns 401 with invalid token', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/boards/xxx/stream?token=invalid-token',
    })
    expect(res.statusCode).toBe(401)
  })

  it('returns 404 for non-existent board with valid token', async () => {
    const plan = await createTestPlan()
    const org = await createTestOrg(plan.id)
    const user = await createTestUser(org.id)
    const authHeader = await getAuthHeader(user.email, 'Test@1234')
    const token = authHeader.replace('Bearer ', '')

    const res = await app.inject({
      method: 'GET',
      url: `/boards/nonexistent-board-id/stream?token=${token}`,
    })
    expect(res.statusCode).toBe(404)
  })
})
```

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
pnpm --filter api test src/modules/stream/stream.test.ts 2>&1 | tail -5
```

Esperado: FAIL — módulo `stream.routes` não encontrado.

- [ ] **Step 3: Criar `apps/api/src/lib/sse.ts`**

```typescript
import { redis } from '@/lib/redis'

export interface SSEEvent {
  event: 'task:moved' | 'task:created' | 'task:updated' | 'comment:added'
  data: Record<string, unknown>
}

export async function publishBoardEvent(boardId: string, payload: SSEEvent): Promise<void> {
  await redis.publish(`board:${boardId}`, JSON.stringify(payload))
}
```

- [ ] **Step 4: Atualizar `apps/api/src/modules/tasks/tasks.service.ts`**

Adicionar o import no topo do arquivo (após os imports existentes):
```typescript
import { publishBoardEvent } from '@/lib/sse'
```

Modificar `verifyColumnBelongsToOrg` para incluir `id` no select do board:
```typescript
async function verifyColumnBelongsToOrg(columnId: string, organizationId: string) {
  const column = await prisma.column.findFirst({
    where: { id: columnId },
    include: { board: { select: { id: true, organizationId: true, clientId: true } } },
  })
  if (!column || column.board.organizationId !== organizationId) {
    throw new AppError(404, 'Coluna não encontrada')
  }
  return column
}
```

Modificar `verifyTaskBelongsToOrg` para incluir `id` no select do board:
```typescript
async function verifyTaskBelongsToOrg(taskId: string, organizationId: string) {
  const task = await prisma.task.findFirst({
    where: { id: taskId },
    include: {
      column: {
        include: { board: { select: { id: true, organizationId: true, clientId: true } } },
      },
    },
  })
  if (!task || task.column.board.organizationId !== organizationId) {
    throw new AppError(404, 'Tarefa não encontrada')
  }
  return task
}
```

Adicionar chamada ao `publishBoardEvent` ao final de `createTask` (antes do `return task`):
```typescript
  await publishBoardEvent(column.board.id, {
    event: 'task:created',
    data: { taskId: task.id, columnId, title: task.title },
  })

  return task
```

Adicionar chamada ao `publishBoardEvent` ao final de `moveTask` (antes do `return updatedTask`):
```typescript
  await publishBoardEvent(toColumn.board.id, {
    event: 'task:moved',
    data: { taskId, fromColumn: fromColumn.id, toColumn: data.columnId, position: data.position },
  })

  return updatedTask
```

Adicionar chamada ao `publishBoardEvent` ao final de `updateTask` (após o `return prisma.$transaction(...)`).

ATENÇÃO: `updateTask` retorna o resultado de `prisma.$transaction` diretamente. Reescrever para capturar o resultado e então publicar:
```typescript
export async function updateTask(
  id: string,
  organizationId: string,
  data: UpdateTaskBody,
  actor: Actor,
) {
  const task = await verifyTaskBelongsToOrg(id, organizationId)
  const actorName = await resolveActorName(actor.id, actor.type)

  const historyEntries: Array<{ action: string; fromValue?: string; toValue?: string }> = []

  if (data.priority !== undefined && data.priority !== task.priority) {
    historyEntries.push({ action: 'priority_changed', fromValue: task.priority, toValue: data.priority })
  }
  if (data.assigneeId !== undefined && data.assigneeId !== task.assigneeId) {
    historyEntries.push({
      action: 'assigned_to',
      fromValue: task.assigneeId ?? undefined,
      toValue: data.assigneeId ?? undefined,
    })
  }

  const updated = await prisma.$transaction(async (tx) => {
    const result = await tx.task.update({
      where: { id },
      data: {
        title: data.title,
        description: data.description,
        priority: data.priority,
        assigneeId: data.assigneeId,
        dueDate:
          data.dueDate === null ? null
          : data.dueDate !== undefined ? new Date(data.dueDate)
          : undefined,
        tags: data.tags,
      },
    })

    for (const entry of historyEntries) {
      await tx.taskHistory.create({
        data: {
          taskId: id,
          action: entry.action,
          fromValue: entry.fromValue,
          toValue: entry.toValue,
          actorType: actor.type,
          actorId: actor.id,
          actorName,
        },
      })
    }

    return result
  })

  await publishBoardEvent(task.column.board.id, {
    event: 'task:updated',
    data: { taskId: id },
  })

  return updated
}
```

- [ ] **Step 5: Atualizar `apps/api/src/modules/comments/comments.service.ts`**

Adicionar o import:
```typescript
import { publishBoardEvent } from '@/lib/sse'
```

Modificar a query em `createComment` para incluir o boardId:
```typescript
export async function createComment(
  taskId: string,
  data: CreateCommentBody,
  actor: CommentActor,
) {
  const task = await prisma.task.findFirst({
    where: { id: taskId, column: { board: { organizationId: actor.organizationId } } },
    include: { column: { include: { board: { select: { id: true } } } } },
  })
  if (!task) throw new AppError(404, 'Tarefa não encontrada')

  const isClient = actor.role === 'CLIENT'
  const comment = await prisma.comment.create({
    data: {
      content: data.content,
      taskId,
      authorType: isClient ? 'CLIENT' : 'USER',
      userId: isClient ? undefined : actor.id,
      clientId: isClient ? actor.id : undefined,
    },
    include: {
      user: { select: { id: true, name: true } },
      client: { select: { id: true, name: true } },
    },
  })

  await publishBoardEvent(task.column.board.id, {
    event: 'comment:added',
    data: { taskId, commentId: comment.id },
  })

  return comment
}
```

- [ ] **Step 6: Rodar a suite da API — deve passar (com mock do redis.publish)**

```bash
pnpm --filter api test src/modules/stream/stream.test.ts --reporter=verbose 2>&1 | tail -10
```

Esperado: 4 testes PASS (o teste de `task:moved` vai precisar do módulo stream registrado — mas os 3 de stream route não, e o 1 de emission usa spy).

**ATENÇÃO:** Os 3 testes de `GET /boards/:id/stream` exigem que a rota exista no servidor. Se falharem com 404 por rota inexistente (não 404 de board), avance para o Step 7 e crie a rota primeiro, depois rode o teste de novo.

- [ ] **Step 7: Rodar suite completa para verificar sem regressão nos serviços**

```bash
pnpm --filter api test 2>&1 | tail -8
```

Esperado: todos os testes existentes passando (o mock de `redis.publish` é restaurado por `vi.restoreAllMocks()` no `afterEach`).

- [ ] **Step 8: Commit**

```bash
git -C /home/max/job/autohubs/tramita add apps/api/src/lib/sse.ts apps/api/src/modules/stream/stream.test.ts apps/api/src/modules/tasks/tasks.service.ts apps/api/src/modules/comments/comments.service.ts
git -C /home/max/job/autohubs/tramita commit -m "feat: SSE lib + event emission em tasks/comments (TDD)"
```

---

## Task 2: Backend — Endpoint SSE stream

**Files:**
- Create: `apps/api/src/modules/stream/stream.routes.ts`
- Modify: `apps/api/src/server.ts`

- [ ] **Step 1: Criar `apps/api/src/modules/stream/stream.routes.ts`**

```typescript
import type { FastifyInstance } from 'fastify'
import { verifyAccessToken } from '@/lib/jwt'
import { prisma } from '@/lib/prisma'
import { redis } from '@/lib/redis'
import { AppError } from '@/errors/AppError'

export async function streamRoutes(app: FastifyInstance) {
  app.get('/boards/:id/stream', async (request, reply) => {
    const { id } = request.params as { id: string }
    const { token } = request.query as { token?: string }

    // Auth via query param — EventSource não suporta headers customizados
    if (!token) throw new AppError(401, 'Token não fornecido')

    let user: ReturnType<typeof verifyAccessToken>
    try {
      user = verifyAccessToken(token)
    } catch {
      throw new AppError(401, 'Token inválido ou expirado')
    }

    // Verificar que o board existe e pertence à org do token
    const board = await prisma.board.findFirst({
      where: { id, organizationId: user.organizationId!, isActive: true },
    })
    if (!board) throw new AppError(404, 'Board não encontrado')

    // Hijack: Fastify não vai fechar a resposta automaticamente
    reply.hijack()
    const raw = reply.raw

    raw.writeHead(200, {
      'Content-Type': 'text/event-stream; charset=utf-8',
      'Cache-Control': 'no-cache',
      'Connection': 'keep-alive',
      'X-Accel-Buffering': 'no',
    })

    // Subscriber Redis dedicado por conexão
    const sub = redis.duplicate()

    sub.on('message', (_channel: string, message: string) => {
      try {
        const parsed = JSON.parse(message) as { event: string; data: unknown }
        raw.write(`event: ${parsed.event}\ndata: ${JSON.stringify(parsed.data)}\n\n`)
      } catch { /* ignorar mensagens malformadas */ }
    })

    await sub.subscribe(`board:${id}`)

    // Heartbeat a cada 30 segundos
    const heartbeat = setInterval(() => {
      raw.write(`event: heartbeat\ndata: ${JSON.stringify({ ts: Date.now() })}\n\n`)
    }, 30_000)

    // Cleanup ao desconectar
    request.raw.on('close', () => {
      clearInterval(heartbeat)
      sub.unsubscribe().catch(() => {})
      sub.quit().catch(() => {})
    })
  })
}
```

- [ ] **Step 2: Registrar em `apps/api/src/server.ts`**

Adicionar import (após os demais imports de módulos):
```typescript
import { streamRoutes } from '@/modules/stream/stream.routes'
```

Adicionar registro (após `app.register(portalRoutes, ...)`):
```typescript
app.register(streamRoutes)
```

- [ ] **Step 3: Rodar os testes do stream para confirmar 4/4 passando**

```bash
pnpm --filter api test src/modules/stream/stream.test.ts --reporter=verbose 2>&1 | tail -12
```

Esperado: 4 testes PASS.

- [ ] **Step 4: Rodar suite completa**

```bash
pnpm --filter api test 2>&1 | tail -5
```

Esperado: sem regressões.

- [ ] **Step 5: Commit**

```bash
git -C /home/max/job/autohubs/tramita add apps/api/src/modules/stream/stream.routes.ts apps/api/src/server.ts
git -C /home/max/job/autohubs/tramita commit -m "feat: SSE stream endpoint — GET /boards/:id/stream?token=<jwt>"
```

---

## Task 3: Frontend — useBoardStream hook + wire nos boards

**Files:**
- Create: `apps/web/src/hooks/useBoardStream.ts`
- Modify: `apps/web/src/pages/app/Board.tsx`
- Modify: `apps/web/src/pages/portal/Board.tsx`

- [ ] **Step 1: Criar `apps/web/src/hooks/useBoardStream.ts`**

```typescript
import { useEffect, useRef } from 'react'
import { useQueryClient } from '@tanstack/react-query'

const BASE_URL = (import.meta.env.VITE_API_URL as string | undefined) ?? 'http://localhost:3000'

export function useBoardStream(boardId: string | undefined) {
  const queryClient = useQueryClient()
  const esRef = useRef<EventSource | null>(null)
  const retryDelay = useRef(1000)

  useEffect(() => {
    if (!boardId) return

    let cancelled = false

    function connect() {
      if (cancelled) return

      const token = localStorage.getItem('accessToken')
      if (!token) return

      const es = new EventSource(`${BASE_URL}/boards/${boardId}/stream?token=${token}`)
      esRef.current = es

      const invalidate = () => {
        queryClient.invalidateQueries({ queryKey: ['board', boardId] })
      }

      es.addEventListener('task:moved', invalidate)
      es.addEventListener('task:created', invalidate)
      es.addEventListener('task:updated', invalidate)
      es.addEventListener('comment:added', invalidate)

      es.addEventListener('heartbeat', () => {
        retryDelay.current = 1000 // reset backoff on successful heartbeat
      })

      es.onerror = () => {
        es.close()
        esRef.current = null
        if (cancelled) return
        const delay = retryDelay.current
        retryDelay.current = Math.min(delay * 2, 30_000)
        setTimeout(connect, delay)
      }
    }

    connect()

    return () => {
      cancelled = true
      esRef.current?.close()
      esRef.current = null
    }
  }, [boardId, queryClient])
}
```

- [ ] **Step 2: Adicionar `useBoardStream` ao `apps/web/src/pages/app/Board.tsx`**

Adicionar o import no topo:
```typescript
import { useBoardStream } from '@/hooks/useBoardStream'
```

Dentro da função `Board()`, após a chamada de `useBoard`:
```typescript
const { boardId } = useParams<{ boardId: string }>()
const { board, isLoading, moveTask } = useBoard(boardId!)
useBoardStream(boardId)   // ← adicionar esta linha
```

- [ ] **Step 3: Adicionar `useBoardStream` ao `apps/web/src/pages/portal/Board.tsx`**

Adicionar o import:
```typescript
import { useBoardStream } from '@/hooks/useBoardStream'
```

Dentro da função `PortalBoard()`, após o `useQuery`:
```typescript
const { boardId } = useParams<{ boardId: string }>()
useBoardStream(boardId)   // ← adicionar esta linha
const { data: board, isLoading } = useQuery<Board>({ ... })
```

- [ ] **Step 4: Verificar build**

```bash
pnpm --filter web build 2>&1 | tail -5
```

Esperado: sem erros TypeScript.

- [ ] **Step 5: Rodar testes frontend — sem regressões**

```bash
pnpm --filter web test 2>&1 | tail -5
```

Esperado: 12 testes passando.

- [ ] **Step 6: Commit**

```bash
git -C /home/max/job/autohubs/tramita add apps/web/src/hooks/useBoardStream.ts apps/web/src/pages/app/Board.tsx apps/web/src/pages/portal/Board.tsx
git -C /home/max/job/autohubs/tramita commit -m "feat: useBoardStream hook — SSE com reconnect backoff + wire nos boards"
```

---

## Task 4: TASKS.md

**Files:**
- Modify: `docs/TASKS.md`

- [ ] **Step 1: Rodar suites completas**

```bash
pnpm --filter api test 2>&1 | tail -5 && pnpm --filter web test 2>&1 | tail -5
```

Esperado: API todos passando, Frontend 12 passando.

- [ ] **Step 2: Atualizar `docs/TASKS.md`**

Localizar a seção `### 8a — Tempo Real (SSE)` e substituir os itens correspondentes:

```markdown
### 8a — Tempo Real (SSE) ✅
- [x] `stream.test.ts` — evento emitido ao mover tarefa, heartbeat (endpoint auth)
- [x] `GET /boards/:id/stream?token=<jwt>` — endpoint SSE
- [x] Emitir eventos: `task:moved`, `task:created`, `task:updated`, `comment:added`
- [x] Frontend interno: conectar ao SSE, atualizar via react-query sem reload
- [x] Portal do cliente: mesma lógica — board atualiza ao vivo
- [x] Reconexão automática com backoff
```

- [ ] **Step 3: Commit**

```bash
git -C /home/max/job/autohubs/tramita add docs/TASKS.md
git -C /home/max/job/autohubs/tramita commit -m "docs: Fase 8a SSE concluída no TASKS.md"
```

---

## Self-Review

### Spec coverage

| Requisito TASKS.md | Task |
|---|---|
| `stream.test.ts` — evento emitido ao mover tarefa | Task 1 |
| `stream.test.ts` — heartbeat a cada 30s | Task 2 (testado via 401/404 auth; heartbeat é comportamento de runtime) |
| `GET /boards/:id/stream?token=<jwt>` | Task 2 |
| Emitir eventos: `task:moved`, `task:created`, `task:updated`, `comment:added` | Task 1 |
| heartbeat | Task 2 (`setInterval` 30s) |
| Frontend interno: SSE + react-query | Task 3 |
| Portal: SSE + react-query | Task 3 |
| Reconexão automática com backoff | Task 3 (`useBoardStream` com exponential backoff) |

### Placeholder scan

Sem placeholders — código completo em todos os steps.

### Type consistency

- `SSEEvent.event` union `'task:moved' | 'task:created' | 'task:updated' | 'comment:added'` consistente em todos os `publishBoardEvent` calls
- `verifyColumnBelongsToOrg` e `verifyTaskBelongsToOrg` agora retornam `board.id` — usado em `createTask`, `moveTask`, `updateTask`
- `useBoardStream(boardId: string | undefined)` — aceita `undefined` para evitar chamada antes do `boardId` estar disponível

### Notas de runtime

- O endpoint SSE não pode ser testado via `app.inject()` para o caminho feliz (conexão fica aberta). Os testes cobrem autenticação (401/404) e a emissão de eventos via spy de `redis.publish`.
- Em produção, o Nginx/Traefik precisa de `X-Accel-Buffering: no` para SSE funcionar sem buffering — header já incluído na resposta.
- `redis.duplicate()` cria um subscriber Redis isolado por conexão — evita que comandos Pub/Sub bloqueiem o cliente Redis principal.
